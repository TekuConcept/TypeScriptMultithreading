# TypeScriptMultithreading

Simple example setup for running multithreaded tasks in NodeJS using TypeScript and worker threads.

## Features

- TypeScript with CommonJS modules
- Worker thread support for multithreading
- Path aliases using tsconfig-paths
- ts-node for development

## Installation

```bash
npm install
```

## Usage

Run the examples:

```bash
npm run examples
```
